rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Permite al administrador leer y escribir en la colección de órdenes
    match /orders/{orderId} {
      allow read, write: if request.auth.uid == 'NvLl6uPHH3MH9qprNtCxkX7IPe23';
    }

  }
}
